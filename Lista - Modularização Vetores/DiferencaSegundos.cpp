#include <stdio.h>
#include <conio.h>
#include <stdlib.h>

void LeTempo(int hora, int min, int seg)
{
	printf("Digite as horas, os minutos e os segundos:\n");
	scanf("%d%d%d", &hora, &min, &seg);
}

int Calculo (&hr, &min, &seg)
{
	hr = hr * 3600;
	min = min * 60;
	seg = seg;
	total = hr + min + seg
}

int main(void)
{
	int hora, min, seg;
	LeTempo(hora, min, seg)
}
